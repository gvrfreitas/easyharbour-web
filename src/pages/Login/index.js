import { React } from 'react';

import { LoginDiv, LoginContainer } from './styles';

function Login() {
  return (
    <LoginDiv>
      <LoginContainer>
      </LoginContainer>
    </LoginDiv>
  );
}

export default Login;
